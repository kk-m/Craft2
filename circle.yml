machine:
    environment:
        ANDROID_HOME: /usr/local/android-sdk-linux
        
dependencies:
    override:
        - echo y | android update sdk --no-ui --filter "android-22,build-tools-23.0.0 rc2"

test:
    override:
        - echo "Nothing to do here"